<h1> All Ingredients </h1>

<ul>
  <% @ingredients.each do |ingredient| %>
  <br>
    <li> <%= link_to ingredient.name, ingredient_path(ingredient) %> </li>
  <%end%>
</ul>

<br><br><br>

<%= button_to('Go To Recipes', recipes_path,
        method: "get")  %>

<%= button_to('Go To Holidays', holidays_path,
        method: "get")  %>